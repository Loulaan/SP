gtest = dependency('gtest', main: true)
test(
    '4',
    executable(
        'Test4',
        sources: ['utests.cpp', 'ptests.cpp'],
        include_directories: src,
        dependencies: [gtest]
    )
)
